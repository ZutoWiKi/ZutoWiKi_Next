/**
 * 홈 "전체 해석글"의 한 페이지 글 수.
 *
 * 서버가 미리 그리는 첫 페이지와 브라우저의 쪽나눔이 같은 값을 써야 해서 한 곳에 둔다.
 * ("use client" 파일에서 export 한 값을 서버에서 import 하면 숫자가 아니라 클라이언트
 * 참조가 되므로, 이런 공용 값은 지시어 없는 파일에 둔다.)
 */
export const HOME_WRITES_PAGE_SIZE = 15;
